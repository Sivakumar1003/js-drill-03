// Find average salary based for each role ( You can find in hr array )

const data = require('../database.js');
const averageSalaryBasedRole = require('../problem2.js');

const result = averageSalaryBasedRole(data);

console.log(result);